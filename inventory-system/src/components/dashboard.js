import "./dashboard.css";

function dashboard(){
    return(
        <div>
            <h1>⚡ Do you want the footer to stick at the bottom even when the page has little content (always visible), or should it only appear after scrolling down (like a normal footer)?</h1>
        </div>
    );
}
export default dashboard;